
<!doctype html>

<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>Create an Account</title>
		<meta name="description" content="form to add new books">
		<meta name="author" content="Mark Tyers">
		<link href="{{host}}/style.css" type="text/css" rel="stylesheet" />
		<script type="module" src="main.js"></script>
	</head>
	<body>
		<header>
			<a class="logo" href="/"><img class="logo_img" src="/logo.png" /></a>
			<h1>Create an Account</h1>
			<a href="/login">Log in</a>
		</header>
		{{#if msg}}
 			<aside>
				<p class="msg">{{msg}}</p>
			</aside>
		{{/if}}
		<main class="main_log_reg">
		<form action="/register" enctype="multipart/form-data" method="post">
			<p>
				<label for="user">Username</label><br />
				<input type="text" name="user" placeholder="e.g. JohnDoe35 (no spaces)" value="{{body.user}}">
			</p>
			<p>
				<label for="pass">Password</label><br />
				<input type="password" name="pass" placeholder="Don't tell anyone" value="{{body.pass}}">
			</p>
			<p>
				<label for="email">Email</label><br />
				<input type="email" name="email" placeholder="e.g. johndoe@gmail.com" value="{{body.email}}">
			</p>
			<p><input type="submit" value="Create"></p>
		</form>
			<p><a href="/">Home page</a></p>
		</main>
		<footer>
			<p>Made by Jonas Djondo</p>
		</footer>
	</body>
</html>
